<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="shortcut icon" type="image/svg+xml" href="/assets/favicons/favicon.svg">
        <link rel="alternate icon" type="image/png" sizes="16x16" href="/assets/favicons/favicon-16x16.png">
        <link rel="mask-icon" href="/assets/favicons/safari-pinned-tab.svg" color="#5bbad5">
        <link rel="apple-touch-icon" sizes="180x180" href="/assets/favicons/apple-touch-icon.png">
        <link rel="manifest" href="assets/manifest.json">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link href="/assets/css/bootstrap.min.css" rel="stylesheet">

        {% endblock %}

        {% block javascripts %}
            <script src="/assets/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </head>
    <body>

            <header>
                <nav class="navbar navbar-expand-lg navbar-dark bg-dark" aria-label="Ninth navbar example">
                    <div class="container-xl">
                        <a class="navbar-brand {{ app.request.get('_route') == 'index' ? 'disabled' }}" href="/">Symfony Store</a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample07XL" aria-controls="navbarsExample07XL" aria-expanded="false" aria-label="Toggle navigation">
                            <span class="navbar-toggler-icon"></span>
                        </button>

                        <div class="collapse navbar-collapse" id="navbarsExample07XL">
                            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                                <li class="nav-item">
                                    <a class="nav-link {{ app.request.get('_route') == 'storeList' ? 'active' }}" href="{{ path('storeList') }}">Catalog</a>
                                </li>
                                <!--li class="nav-item">
                                    <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
                                </li-->

                                <!--li class="nav-item dropdown d-inline-flex">
                                    <a class="nav-link {{ app.request.get('_route') == 'storeList' ? 'active' }}" href="{{ path('storeList') }}">Same Catalog</a><button type="button" class="dropdown-toggle my-auto bg-transparent border-0 text-white" id="dropdown07XL" data-bs-toggle="dropdown" aria-expanded="false"></button>
                                    <ul class="dropdown-menu" aria-labelledby="dropdown07XL">
                                        <li><a class="dropdown-item" href="{{ path('storeList') }}">Same Catalog</a></li>
                                        <li><a class="dropdown-item" href="{{ path('storeList') }}">Same Catalog</a></li>
                                        <li><a class="dropdown-item" href="{{ path('storeList') }}">Same Catalog</a></li>
                                    </ul>
                                </li-->
                            </ul>
                            <!--form>
                                <input class="form-control" type="text" placeholder="Search" aria-label="Search">
                            </form-->
                        </div>
                        <li class="nav-item d-inline-block">
                                    <a class="nav-link btn btn-danger {{ app.request.get('_route') == 'cart' ? 'disabled' }}" href="{{ path('storeCart') }}" tabindex="-1" aria-disabled="true">Cart</a>
                        </li>
                    </div>
                </nav>
            </header>

            {% block body %}{% endblock %}
    </body>
</html>
